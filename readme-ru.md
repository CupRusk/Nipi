# `nipi` — Мини CLI-утилита на Nim

`nipi` — лёгкая консольная утилита на Nim для управления мини-проектами через команды CLI.

---

## 🔹 Установка

1. Склонируйте репозиторий:

```bash
git clone https://github.com/yourusername/nipi.git
cd nipi
```

2. Убедитесь, что Nim установлен:

```bash
nim -v
```

3. Компиляция:

```bash
nim c -r nipi.nim
```

> Флаг `-r` сразу запускает программу после компиляции.

---

## 🔹 Использование

Программа поддерживает следующие команды:

| Команда         | Описание                                          |
| --------------- | ------------------------------------------------- |
| `list`          | Вывод всех установленных проектов/операций        |
| `create <name>` | Создание нового проекта с именем `<name>`         |
| `help`          | Вывод справки с доступными командами              |
| `help -s`       | Вывод справки по шагам с задержкой между строками |

### Примеры

```bash
./nipi
# Вывод: Операция не существует :3
```

```bash
./nipi
help
# Вывод:
# Все возможные операции:
#    1. list - Вывод всего, что вы установили
#    2. create <name> - Создание проекта
```

```bash
./nipi
help -s
# Вывод с задержкой в 2 секунды между строками
```

---

## 🔹 Особенности

* Использует **цветной вывод** через `terminal.styledEcho`
* Минимальный CLI с разбором аргументов
* Простая структура для расширения (легко добавить новые команды)

---

## 🔹 Структура проекта

```
nipi/
├─ Lexer.nim       # Модуль для токенизации команд
├─ main.nim        # Главный исполняемый файл
├─ README.md       # Документация
```

---

## 🔹 Зависимости

* Nim (рекомендуется последняя версия)
* Модуль `terminal` (входит в стандартную библиотеку Nim)
* Модуль `os` (для работы с файловой системой и задержкой)

